apiVersion: forma.dev/v1alpha1
kind: Form
metadata:
  name: checklist-result-create
  module: crc
  description: Form isi hasil checklist
spec:
  entity: checklist-result
  mode: create
  render: separate_page
  sections:
    - title: "Informasi Inspeksi"
      fields:
        - { field: inspection_date, rules: [required] }
        - { field: work_order_id, rules: [required] }
        - { field: template_id, rules: [required] }
        - { field: inspected_by, rules: [required] }
    - title: "Item Checklist"
      fields:
        - { field: items }
    - title: "Kesimpulan"
      fields:
        - { field: overall_result, rules: [required] }
        - { field: notes }
