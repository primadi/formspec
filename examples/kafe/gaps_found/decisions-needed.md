# Keputusan Semantik (D1–D7) — Hasil Fase 0

> Menutup `TODO.md` **0.3**. Dijawab dari **kode**, bukan dari prosa dokumen.
> Kolom "Diputuskan oleh" menandai siapa yang berwenang mengubahnya.

## Ringkasan

| #      | Pertanyaan                                                             | Jawaban (kanonik)                                                                                                                                                                                                  | Bukti                                                                                                                                                                 | Diputuskan oleh                         |
| ------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| **D1** | Apakah `create` selalu menghasilkan `doc_status: draft`?               | **Ya — kecuali entity _lifecycle-free_** (lihat D2). `draft` tidak bisa jadi target `relation`.                                                                                                                    | `renderers/jsonb-persist/crud.go:510-513` — `initialDocStatus := "NULL"; if s.submitEnabled { "'draft'" }`                                                            | Platform (sudah berlaku)                |
| **D2** | Beda `lifecycle: plain_crud` dengan tidak mendeklarasikan `lifecycle`? | **Tidak ada bedanya untuk `doc_status`.** `lifecycle:` adalah _hint UI_; penentu lifecycle adalah **apakah aksi `submit` aktif**. Semua `submit`+`cancel`+`amend` nonaktif ⇒ _lifecycle-free_ (`doc_status NULL`). | `pkg/spec/entity.go:100` (`// two_step_autosave \| two_step_manual \| plain_crud (default derived from actions)`); `crud.go:510`; `internal/api/generator.go:152-156` | Platform — **perlu dipertegas di spec** |
| **D3** | Apakah transisi state machine memancarkan event otomatis?              | **Tidak.** Event diikat ke **aksi/hook**, bukan ke transisi. `TransitionDecl` tidak punya field `emit`.                                                                                                            | `pkg/spec/entity.go:708-712` (`TransitionDecl{From,To,Action,Guard}`); `ValidateActionEmits`                                                                          | Platform — **gap S13**                  |
| **D4** | Konvensi nama event: nama state atau prefix `on_*`?                    | **Prefix.** `before_*` ⇒ sync (wajib), `on_*` ⇒ async (wajib), nama lain ⇒ `type` **wajib eksplisit**. Nama state polos (mis. `order.paid`) **bukan** konvensi yang ditegakkan.                                    | `pkg/spec/entity.go:921-955` (`ValidateEventNaming`)                                                                                                                  | Platform (sudah berlaku)                |
| **D5** | Permission = `module.<entity>.action` atau `module.<plural>.action`?   | **Kaninik: `{module}.{plural}.{action}`.** Namun ada jalur yang memakai **singular** → tidak akan pernah cocok.                                                                                                    | Kanonik: `internal/api/generator.go:173`, `internal/entity/registry.go:218-225`. Menyimpang: `internal/api/file.go:451` (+ pesan di :149/:356/:560/:721/:803/:863)    | Platform — **BUG, lihat #53**           |
| **D6** | Apakah `submit` butuh permission `update`?                             | **Jawaban sebenarnya: `submit` punya permission sendiri** (`{module}.{plural}.submit`). Pesan "butuh `update`" yang terlihat di runtime berasal dari **bug routing** (#52), bukan aturan.                          | `internal/api/descriptor.go:52` (`PermissionAction: "submit"`); pesan runtime berasal dari rute file (`file.go:149`)                                                  | Platform — koreksi catatan lama         |
| **D7** | Di mana `settings` hidup & siapa membacanya?                           | Di `kind: Config` level App (`spec/config/app.yaml`), dibaca `pkg/spec/money.go` → `ResolveMoneyCurrency`. **Namun belum diterapkan** di jalur API (lihat #46).                                                    | `examples/kafe/spec/config/app.yaml`; `pkg/spec/money.go`                                                                                                             | Platform — **gap #46**                  |

## Yang berubah dari catatan lama

| Catatan lama                              | Koreksi Fase 0                                                                                                             |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| D1 "create → draft" (diverifikasi)        | Benar, **tapi** ada jalur keluar: entity _lifecycle-free_ ⇒ `doc_status NULL` ⇒ langsung _referenceable_.                  |
| D2 "keduanya tampak memakai `doc_status`" | Benar, dan **sekarang jelas kenapa**: `lifecycle:` tidak dibaca untuk keputusan ini.                                       |
| D6 "submit butuh permission `update`"     | **Salah.** `submit` punya permission sendiri; pesan `update` adalah artefak bug routing (#52) + permission singular (#53). |
| D4 "kontradiktif"                         | **Tidak kontradiktif.** `on_*` kanonik; dokumen vertical yang memakai nama state perlu dikoreksi.                          |

## Implikasi langsung ke TODO.md

| Temuan                                                                              | Item TODO yang terdampak                                                                                                              |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **D2** — lifecycle-free bisa dicapai dengan menonaktifkan `submit`+`cancel`+`amend` | **2.1** — jangan tambah `lifecycle: none` bila mekanisme yang ada sudah cukup; cukup **dokumentasikan** + pastikan tereksekusi benar. |
| **D6** dikoreksi                                                                    | Permintaan `submit` tidak perlu permission `update` — hilangkan asumsi ini dari spec kafe.                                            |
| **D5**                                                                              | Perbaiki `internal/api/file.go` agar memakai `plural` (lihat #53).                                                                    |
| **D3/D4**                                                                           | Menegaskan **S13** (butuh `emit:` pada transisi) dan **#40**.                                                                         |
