apiVersion: formspec.dev/v1
kind: Entity
metadata:
  name: account
  module: gl
  description: Chart of accounts — satu bagan akun standar per workspace
spec:
  version: v1
  characteristic: reference

  fields:
    - name: code
      type: string
      natural_key: true
      # Kode akun dipasok seed bagan akun (dan boleh diisi user saat menambah
      # akun sendiri) — engine TIDAK pernah men-generate-nya. `auto_generated`
      # akan mengabaikan nilai yang dikirim, yang salah untuk field ini.
      natural_key_entry: user_entry
      immutable: true
      index: true
      description: Kode akun — "1-10001" (Kas), "4-40001" (Pendapatan)
    - name: name
      type: string
      rules: [required]
      description: Nama akun — "Kas", "Pendapatan Penjualan"
    - name: type
      type: enum
      enum_values: [asset, liability, equity, revenue, expense]
      index: true
    - name: normal_balance
      type: enum
      enum_values: [debit, credit]
      description: Saldo normal — asset=debit, revenue=credit
    - name: is_active
      type: boolean
      default: true
