# Total liabilitas — Σ saldo akun bertipe liabilitas pada periode berjalan.
# DIPERBAIKI 2026-09-22: entity `ledger` tidak pernah ada; sumber sebenarnya
# adalah `gl-balance` (lihat catatan di total-assets.yaml).
apiVersion: formspec.dev/v1
kind: Widget
metadata:
  name: total-liabilities
  module: gl
  description: "Total liabilitas — Σ closing_balance akun bertipe liability"
spec:
  title: "Total Liabilitas"
  type: metric
  entity: gl.gl-balance
  query: "period = 'this-month'"
  refresh_secs: 300
  config:
    aggregate: sum
    field: closing_balance
    format: currency
