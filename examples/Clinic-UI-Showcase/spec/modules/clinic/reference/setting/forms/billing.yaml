# SHOWCASE: form kedua Configuration Page (tab "Billing & Struk"), row id="billing".
apiVersion: formspec.dev/v1
kind: Form
metadata:
  name: settings-billing
  module: clinic
  description: Pengaturan pajak & struk
spec:
  entity: setting
  render: separate_page
  sections:
    - title: "Pajak"
      fields:
        - { field: tax_percent, label: "PPN (%)" }
    - title: "Struk"
      fields:
        - { field: receipt_footer, label: "Footer Struk" }
  submit:
    label: "Simpan Pengaturan"
