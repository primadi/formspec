# SHOWCASE kind: Form — modal quick-create untuk penjualan OTC. Alur: create
# (status pending) → kasir klik action "sell" di halaman record untuk
# menyelesaikan transaksi (idiom "create idle, script action finalizes" yang
# sama dipakai visit/prescription).
apiVersion: forma.dev/v1alpha1
kind: Form
metadata:
  name: otc-sale-quick
  module: pharmacy
  description: Penjualan obat bebas — quick create
spec:
  entity: otc-sale
  render:
    mode: modal
  sections:
    - title: "Penjualan"
      fields:
        - { name: transaction_date, label: "Tanggal" }
        - { name: patient_id, label: "Pasien Terdaftar", help: "Opsional" }
        - { name: buyer_name, label: "Nama Pembeli", placeholder: "Opsional — boleh kosong untuk pembeli anonim" }
        - { name: items, label: "Item", widget: child-grid }
        - { name: notes, label: "Catatan" }
