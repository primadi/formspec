apiVersion: forma.dev/v1alpha1
kind: Module
metadata:
  name: billing
  description: Minimal registry-test fixture — 2 master entities, nothing else
spec:
  version: 1.0.0
  depends:
    - module: forma/core
