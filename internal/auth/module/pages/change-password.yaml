apiVersion: formspec.dev/v1
kind: Page
metadata:
  name: change-password
  module: formspec.core
  description: "Default change-password page — self-service password change (plan auth-screens-spec-driven)"
spec:
  route: /_auth/change-password
  mode: custom
  asset: formspec-core/auth/change-password