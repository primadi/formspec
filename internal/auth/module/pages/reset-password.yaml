apiVersion: formspec.dev/v1
kind: Page
metadata:
  name: reset-password
  module: formspec.core
  description: "Default reset-password page — email link with token (plan auth-screens-spec-driven)"
spec:
  route: /_auth/reset-password
  mode: custom
  asset: formspec-core/auth/reset-password