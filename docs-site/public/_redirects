# Cloudflare Pages redirects — docs.formspec.dev

# Semua route VitePress sudah pre-rendered (SSG), tidak perlu fallback.

# CATATAN: redirect www.docs.formspec.dev → docs.formspec.dev TIDAK bisa lewat

# \_redirects (domain redirect tidak didukung) — gunakan Redirect Rule dashboard.
