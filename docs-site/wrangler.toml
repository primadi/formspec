# Cloudflare Pages — deployment config untuk docs.formspec.dev
# Deploy command: npx wrangler deploy (flow wrangler baru → pakai [assets]).
name = "formspec-docs"
compatibility_date = "2026-08-12"

[assets]
directory = "./dist"
