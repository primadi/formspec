# forma-operator Deployment (docs/runtimes/03-forma-operator.md §5).
# 2 replicas with leader election: one active, one standby.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forma-operator
  namespace: forma-system
  labels:
    app.kubernetes.io/name: forma-operator
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: forma-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: forma-operator
    spec:
      serviceAccountName: forma-operator
      containers:
        - name: operator
          image: registry.forma.dev/forma-operator:0.1.0
          args:
            - --leader-elect
            - --control-cluster-url=$(CONTROL_CLUSTER_URL)
            - --resource-image=formahub/forma-resource:0.1.0
          env:
            - name: CONTROL_CLUSTER_URL
              value: ""
          ports:
            - name: metrics
              containerPort: 8443
            - name: probes
              containerPort: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: probes
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: probes
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
