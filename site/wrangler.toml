# Cloudflare Pages — deployment config untuk landing formspec.dev
# Deploy command: npx wrangler deploy (flow wrangler baru → pakai [assets],
# bukan pages_build_output_dir yang legacy untuk `wrangler pages deploy`).
name = "formspec-site"
compatibility_date = "2026-08-12"

[assets]
directory = "./dist"
