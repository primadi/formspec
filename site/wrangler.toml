# Cloudflare Pages — deployment config untuk landing formspec.dev
# Build via: npm install && npm run build
# Output: site/dist (dikirim oleh Cloudflare Pages build pipeline)
name = "formspec-site"
compatibility_date = "2026-08-12"
pages_build_output_dir = "dist"
