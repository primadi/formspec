# Archive lock — locked_for_deletion (todo 4.9.3, 4.9.4)

**Date**: 2026-08-17

Mengimplementasikan enforcement `locked_for_deletion` pada master yang
direferensikan transaksi terarsip.

- `renderers/jsonb-persist/crud.go`: `SoftDelete` memblokir delete bila
  `data.locked_for_deletion == true` dengan
  `spec.ErrorArchiveLockedForDeletion` (`FORMSPEC.ARCHIVE.LOCKED_FOR_DELETION`).
- Test: `renderers/jsonb-persist/archive_lock_test.go` (delete normal diizinkan,
  delete record ber-flag diblokir).

Catatan: 4.9.1 (archive ke Parquet), 4.9.2 (master snapshot as-of), dan 4.9.5
(`formspec archive run/view/restore-batch`) tetap item terpisah — flag
`locked_for_deletion` di-set oleh archive run (belum ada).
