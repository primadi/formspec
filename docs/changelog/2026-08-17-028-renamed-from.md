# renamed_from field (todo 4.2.2)

**Date**: 2026-08-17

Menambahkan deklarasi rename field.

- `pkg/spec/entity.go`: `Field.RenamedFrom` + validasi di `ValidateEntitySpec`
  (tidak boleh reserved name, tidak boleh collide dengan field existing).
- Test: `pkg/spec/entity_test.go` `TestValidateEntitySpec_RenamedFrom`.

Catatan: diff field-add (4.2.1) tidak menandai kolom lama sebagai removal —
rename diperlakukan sebagai add, bukan drop+add. Drop dua-fase penuh
(deprecate lalu remove lintas dua versi apply) tetap enhancement.
