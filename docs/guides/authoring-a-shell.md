# Membangun Shell Baru

**Status:** Outline

> Outline: heading menetapkan cakupan; isi ditulis setelah kontrak Spec
> Resolution API berstatus Draft.

## 1. Kelas Investasi
Shell = interpreter penuh atas kontrak kind system (Layer 0 auto-generate,
navigation, auth wiring, permission-aware rendering) — bukan konsumen satu
endpoint. Kebijakan: first-party dulu, terbuka setelah terbukti.

## 2. Kontrak yang Wajib Dipenuhi
Spec Resolution API, hirarki App/Page/Component, FormaExpr, permission
filtering.

## 3. Pemetaan Katalog Renderer
Mengisi katalog app/page/component kind dengan widget native platform target.

## 4. Navigation Model
Adaptasi model navigasi per platform (URL-based vs stack-based).

## 5. Konformansi
Cara memverifikasi shell memenuhi kontrak.
