apiVersion: forma.dev/v1alpha1
kind: App
metadata:
  name: billing
  description: Order-to-cash — customer, order, checkout, payment gateway
spec:
  version: 1.0.0
  vendor: forma-dev
  root_url: /app/billing
  modules: [billing]
  menu:
    - type: module
      module: billing
  publishes:
    - service: order-events
      actions: [read]
