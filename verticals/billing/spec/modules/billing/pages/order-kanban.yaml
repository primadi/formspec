# Order kanban board — drag-and-drop by status
apiVersion: formspec.dev/v1
kind: Page
metadata:
  name: order-kanban
  module: billing
  description: Board order — drag-and-drop antar status
spec:
  route: /app/orders/board
  title: "Board Order"
  icon: "columns"
  permissions: [orders.list]
  blocks:
    - component:
        ref: order-kanban-board
        entity: order
