# Invoice print template — multi-format output
apiVersion: formspec.dev/v1alpha1
kind: Print
metadata:
  name: invoice-print
  module: billing
  description: Template cetak invoice/receipt — PDF, thermal, HTML
spec:
  entity: invoice
  template: invoice-standard
  formats: [pdf, thermal, html]
