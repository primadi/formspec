apiVersion: formspec.dev/v1
kind: Module
metadata:
  name: sales-inventory-integrator
  description: Connects billing (sales) to inventory — no entities of its own, Subscription only
spec:
  version: 1.0.0
  depends:
    - module: formspec/core
