apiVersion: formspec.dev/v1alpha1
kind: Module
metadata:
  name: company
  description: Org structure — branch directory shared by other verticals
spec:
  version: 1.0.0
  depends:
    - module: formspec/core
