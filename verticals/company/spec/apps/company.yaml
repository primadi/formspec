apiVersion: formspec.dev/v1
kind: App
metadata:
  name: company
  description: Shared org-structure — branch directory used by other verticals
spec:
  version: 1.0.0
  vendor: formspec-dev
  root_url: /app/company
  modules: [company]
  publishes:
    - service: branch-directory
      actions: [read]
