# Binaries
bin/
obj/
*.exe
*.dll
*.so
*.dylib

# Compiled CLI binaries at repo root (output of `go build ./cmd/...`)
/formspec
/formspec-gen-schema

# Test binary, built with `go test -c`
*.test

# Output of go coverage
*.out
coverage.html

# FormSpec runtime data
.formspec/

# Dependencies
# Root-anchored: only ignores a root-level Go vendor/ dir. internal/vendor/
# is a real Go package (module vendoring) and MUST be committed.
/vendor/
sdk/php/vendor/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Go build output & release artifacts (dist/release, lihat docs/guides/releasing.md)
dist/
# Embedded SPA salinan renderers/react-shadcn/dist — di-build saat make build/release,
# tidak di-commit (go:embed via build tag formspec_spa, lihat cmd/formspec/spa_embed.go)
cmd/formspec/dist/
cmd/formspec-registry/web/dist/

# Node (frontend)
node_modules/
sdk/typescript/dist/
sdk/browser/dist/

# Landing site (site/ = Vite+React)
site/dist/
site/node_modules/

# Docs site (docs-site/ = VitePress)
docs-site/node_modules/
docs-site/dist/
# Symlink build-time docs-site/docs -> ../docs (dibuat saat setup, lihat docs-site/README.md)
docs-site/docs